@model IEnumerable<GAP.Models.Facture>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Prix)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FournisseurEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Validite)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Prix)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FournisseurEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Validite)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FactureID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FactureID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FactureID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
