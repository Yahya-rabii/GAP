@model IEnumerable<GAP.Models.OrdreCreationFacture>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Devis)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RespServiceFinanceId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Devis.DevisID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RespServiceFinanceId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrdreCreationFactureID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrdreCreationFactureID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrdreCreationFactureID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
