@model IEnumerable<GAP.Models.RapportTestQualite>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ValiditeEtat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValiditeNbrPiece)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValiditeFonctionnement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RespServiceQualite)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ValiditeEtat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValiditeNbrPiece)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValiditeFonctionnement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RespServiceQualite.Discriminator)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
